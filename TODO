TODO
====

The following is an unordered list of thing I think would be good to add:

- From the bindings yaml file add required dts properties
- From the bindings yaml file add optional dts properties
